[build-system]
requires=["setuptools>=69"]
build-backend="setuptools.build_meta"
[project]
name="shellcheckr"
version="0.1.0"
description="Lightweight shell script linter"
requires-python=">=3.10"
[project.scripts]
shellcheckr="shellcheckr.cli:main"
[tool.setuptools.packages.find]
where=["src"]
